'use client';

import React from 'react';
import { SubscriptionManagerContainer } from './SubscriptionManagerContainer';

export const SubscriptionManager: React.FC = () => {
  return <SubscriptionManagerContainer />;
};
